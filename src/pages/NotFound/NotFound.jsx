import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        marginTop: "2rem",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "2rem",
      }}
    >
      404 | NotFound
    </div>
  );
};

export default NotFound;
